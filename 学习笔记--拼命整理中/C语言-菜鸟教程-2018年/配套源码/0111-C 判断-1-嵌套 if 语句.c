// 代码格式化对齐的快捷键是: ctrl + shift + a 
#include <stdio.h>
int main() {
	int a = 100;
	int b = 200;
	if(a==100) {
		if(b==200) {
			printf("a的值是 100，且b的值为200\n");
		}
	}
	printf("a 的准确值是 %d\n", a );
	printf("b 的准确值是 %d\n", b );
	return 0;
}
